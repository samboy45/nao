{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/swans.css') }}">
    <link rel="stylesheet" href="{{ asset('css/navbar-transparente.css') }}">
{% endblock %}

{% block body %}
<div class="row bg-transparent">
    <div class="col-xs-12 text-center" id="home-1">

        <div class="row">
            <div class="col-xs-offset-1 col-xs-10 col-sm-offset-2 col-sm-8 col-md-offset-3 col-md-6">
                <h1 class="text-uppercase blanc visible-lg">bienvenue sur le site de l'association nos amis les oiseaux</h1>
                <h2 class="text-uppercase blanc visible-md">bienvenue sur le site de l'association nos amis les oiseaux</h2>
                <h3 class="text-uppercase blanc visible-sm">bienvenue sur le site de l'association nos amis les oiseaux</h3>
                <h4 class="text-uppercase blanc visible-xs">bienvenue sur le site de l'association nos amis les oiseaux</h4>
                <p class="blanc">
                    Comme de nombreux passionés, participez à notre programme national de recherche grâce à notre outil collaboratif d'observation des oiseaux
                </p>
            </div>

        </div>
        <div class="row">
            <div class="col-xs-4 col-xs-offset-4 col-sm-6 col-sm-offset-3 jumelles">
                <a href="{{ path('recherche') }}">
                    <img src="{{ asset('imgs/bouton-xs-1.png') }}" class="img-responsive visible-xs">
                </a>
                <a href="{{ path('observation_new') }}">
                    <img src="{{ asset('imgs/bouton-xs-2.png') }}" class="img-responsive visible-xs">
                </a>
                <a href="{{ path('recherche') }}">
                    <img src="{{ asset('imgs/bouton-md-1.png') }}" class="img-responsive hidden-xs">
                </a>
                <a href="{{ path('observation_new') }}">
                    <img src="{{ asset('imgs/bouton-md-2.png') }}" class="img-responsive hidden-xs">
                </a>
            </div>
        </div>
        <br>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/documentSize.js') }}"></script>
{% endblock %}
